mixin formInput(type, name, placeholder, isRequired)
    input.form-input.rounded-0.form-control.border-chinese-silver(type=type, name=name, placeholder=placeholder, id=name, required=isRequired)&attributes(attributes)
